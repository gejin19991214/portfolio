import { Box } from "@chakra-ui/react";
import styled from "@emotion/styled";

export const InternSection = styled(Box)`
    font-size: 0.8em;
    padding-left: 3.4em;
    text-indent: -3.4em;
`

export const InternYear = styled.span`
    font-size: 0.8em;
    font-weight: bold;
    margin-right: 1em;
`